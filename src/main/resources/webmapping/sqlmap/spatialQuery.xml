<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 

<mapper namespace="OpenGDSMobileMapper">	
	<select id="attrTable" parameterType="java.util.HashMap" resultType="java.util.LinkedHashMap">
		select * from ${tableName} 
		<!-- where name='구로구' -->
		<!-- select * from seoul_env_position -->
	</select>	
	<select id="reamTimeTable" parameterType="java.util.HashMap" resultType="java.util.LinkedHashMap">
		select * from "REALTIMEINFO"
	</select>		
	<select id="reamTimeTableColum" parameterType="java.util.HashMap" resultType="java.util.LinkedHashMap">
		select ${column} from "REALTIMEINFO"
	</select>
</mapper>